image: node:18

pages:
  stage: deploy
  cache:
    paths:
      - frontend/node_modules/
  script:
    - cd frontend
    - npm install
    - npm run build
    - mv dist ../public
  artifacts:
    paths:
      - public
  only:
    - main
